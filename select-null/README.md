# Select Null

**🔗 Problem Link:** [View on NeetCode](https://neetcode.io/problems/sql-select-null/question)

---

## 📋 Problem Description

We can also return rows where a column value is NULL or NOT NULL.

The first query will return all rows where the difficulty column is NULL, meaning it is empty.

The second query will return all rows where the difficulty column is NOT NULL, meaning it is not empty. Important: To compare a column to NULL, we cannot use the = or != operators. It won't behave as expected. We must use the IS NULL or IS NOT NULL operator. ChallengeYou are given a table called comments. Return all rows where the author column is not NULL.

---

## 💡 Solution

Check the `solution.py` file in this directory for the complete implementation.

---

## 📊 Complexity Analysis

*Add your complexity analysis here after solving*

- **Time Complexity:** O(?)
- **Space Complexity:** O(?)

---

<sub>This problem was automatically synced from NeetCode using [NeetCode GitHub Pusher](https://github.com/)</sub>
